#include<stdio.h>
#include<conio.h>
int main()
{
int year;
printf("enter any year:");
scanf("%d",&year);
if(year%4==0)
printf("\n yera is a leap year%d",year);
else
printf("\n year is not leap year %d",year);

}
